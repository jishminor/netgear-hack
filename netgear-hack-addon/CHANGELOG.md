# Changelog

## 0.1.0

- Initial Home Assistant add-on scaffold
- HTTP API for `block`, `unblock`, and `health`
- Router configuration via addon options
- Self-contained addon build context
- Python runtime installed explicitly in the addon image
